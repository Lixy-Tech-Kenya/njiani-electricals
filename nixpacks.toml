[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@10 && pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @njiani/backend exec prisma generate",
  "pnpm --filter @njiani/backend build"
]

[start]
cmd = "node apps/backend/dist/main"
