import { CategoryForm } from '../_components/CategoryForm';

export const metadata = { title: 'New Category — Njiani Admin' };

export default function NewCategoryPage() {
  return <CategoryForm />;
}
